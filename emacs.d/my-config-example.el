;;; package --- Summary
;;; Commentary:

;;; Ths is where all config variables will be placed to keep them out of the repo

;;; Code:

(defvar config-gopath "~/go")
(defvar config-gobin (concat config-gopath "/bin"))
(defvar config-theme 'zenburn)
(defvar config-org-files "~/Google Drive/Documents/Org")
(defvar config-shell "/bin/bash")

;;; my-config-example.el ends here
